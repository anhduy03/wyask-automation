# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: src/tests/plus-plan/subscription.feature.spec.js >> Subscription Settings (Plus Plan) >> Plus user sees Upgrade to Pro option
- Location: .bdd-gen/src/tests/plus-plan/subscription.feature.spec.js:10:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: /Upgrade to Pro/i })
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByRole('button', { name: /Upgrade to Pro/i })

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - region "Notifications alt+T"
  - generic [ref=e3]:
    - heading "404" [level=1] [ref=e4]
    - heading "Page Not Found" [level=2] [ref=e5]
    - paragraph [ref=e6]: Sorry, we couldn't find the page you're looking for. The page may have been moved or deleted.
    - link "Back to Home" [ref=e8] [cursor=pointer]:
      - /url: /
  - generic:
    - dialog:
      - generic:
        - generic:
          - heading [level=2]: Unsaved Changes
      - generic:
        - button: Leave
        - button: Stay
      - button:
        - img
  - alert [ref=e9]
```

# Test source

```ts
  1  | import { createBdd } from 'playwright-bdd';
  2  | import { test, expect } from '../../fixtures/commonHelper';
  3  | import { SubscriptionSettingsPage } from '../../pages/SubscriptionSettingsPage';
  4  | 
  5  | const { Given, Then } = createBdd(test);
  6  | 
  7  | Given('the user is on the subscription settings page', async ({ subscriptionSettingsPage }) => {
  8  |   await subscriptionSettingsPage.goto();
  9  |   await test.info().page?.waitForTimeout(3000);
  10 | });
  11 | 
  12 | Then('the {string} button should be visible', async ({ page }, buttonName: string) => {
  13 |   const button = page.getByRole('button', { name: new RegExp(buttonName, 'i') });
> 14 |   await expect(button).toBeVisible({ timeout: 10000 });
     |                        ^ Error: expect(locator).toBeVisible() failed
  15 | });
  16 | 
  17 | Then('the {string} button should not be visible', async ({ page }, buttonName: string) => {
  18 |   const button = page.getByRole('button', { name: new RegExp(buttonName, 'i') });
  19 |   await expect(button).not.toBeVisible({ timeout: 5000 });
  20 | });
  21 | 
  22 | Then('the user should see the current plan indicator', async ({ subscriptionSettingsPage }) => {
  23 |   await expect(subscriptionSettingsPage.currentPlanButton).toBeVisible();
  24 | });
  25 | 
```